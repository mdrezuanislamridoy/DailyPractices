we can grouping individual object into a single entity by collections framework
        Collections FrameWork
        -- Collection(I), Map(I) , Others

        -- COllections(I) // I means Interface
            ||  ||  ||
            ||  ||  ||
            Type Of Collection Interface is
         List ,Set and queue

        List is 3 type
        Array List , Linked List , Vector { Vector have Stack}


        Set(I) is 2 type
        Hash set, {Hashset have Linked Hash Set}
        Shorted set { Navigable interface ( TreeSet)}

        Queue is two types
        Priority Queue, BlockingQueue{ Priority Blocking Queue, Linked Blocking Queue, Other Classes}



        Map(I)
        HashMap{LinkedHashMap}, WeekHashMap, IdentityHashMap, Hashtable{Properties},ShortedMap{NavigableMap(TreeMap)}

        Others
        [Cursors{Enumiration,Iterator(ListIterator)}, Shorting{Comparable,Comparator},Utility Classes{Collections,Arrays}]

        